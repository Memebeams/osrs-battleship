{
  "name": "board",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "src/lib/board/src",
  "prefix": "bs",
  "projectType": "library",
  "tags": [],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "src/lib/board/jest.config.ts",
        "tsConfig": "src/lib/board/tsconfig.spec.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
